{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 3001",
    "start": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port 3001",
    "build": "python3 -m compileall app",
    "lint": "python3 -m py_compile app/main.py"
  }
}
