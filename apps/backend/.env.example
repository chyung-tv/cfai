# Backend Environment Variables (apps/backend)
# For Motia Cloud deployment

# ============================================
# DATABASE
# ============================================
# Use the same Vercel Postgres URL as frontend
DATABASE_URL="postgresql://user@localhost:5432/postgres?schema=public"

# ============================================
# AI & EXTERNAL APIS
# ============================================
# Google AI (Gemini) API Key
GOOGLE_API_KEY="your-google-ai-api-key"

# Financial Modeling Prep API
FMP_API_KEY="your-fmp-api-key"
FMP_BASE_URL="https://financialmodelingprep.com/api/v3"

# ============================================
# MOTIA CLOUD DEPLOYMENT
# ============================================
# 
# Deploy via Web Interface (Recommended):
# 1. Start local: pnpm dev (runs on port 3001)
# 2. Visit: motia.cloud → "Import from Workbench"
# 3. Select port 3001
# 4. Upload this .env file or paste variables
# 5. Click Deploy
#
# Deploy via CLI:
# motia cloud deploy \
#   --api-key $MOTIA_API_KEY \
#   --version-name 1.0.0 \
#   --env-file .env.production
#
# Note: Redis and BullMQ are auto-configured by Motia Cloud
# No need to set REDIS_HOST or REDIS_PORT manually
